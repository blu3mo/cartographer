module Web.Orphans where

-- This file previously contained orphan instances for Newtypes that are now Type Aliases.
-- Since UserId is now a type alias for UUID, we rely on the upstream instances for UUID.
