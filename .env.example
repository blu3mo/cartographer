# LLM API Configuration
OPENROUTER_API_KEY="your_openrouter_api_key_here"

# Supabase Setup
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

SUPABASE_SERVICE_ROLE_KEY=

# PostgreSQL Direct Connection (for Backend Projection)
# Local development (default supabase local):
# データベース接続文字列 (例: ローカルの Docker / Supabase)
DATABASE_URL="postgresql://postgres:password@localhost:54322/postgres?sslmode=disable"

# Process Compose 用ポート番号 (flake.nix でもデフォルトが指定されています)
PC_PORT_NUM=9999